# `extra_json` schemas

The category-specific documentation and machine-readable JSON Schemas are in
[`extra_json_schemas/`](extra_json_schemas/README.md). They were inferred from
all 610 rows of the frozen `final_metadata.csv`, including the clean category
and all 12 corruption categories. Generator semantics and units are documented
in the accompanying Markdown file for each category.
